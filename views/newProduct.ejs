<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("./partials/header.ejs") %>
        <title>New Recipie</title>

        <style>
            form {
                margin: 0 auto;
                width: 50%;
                border: 3px solid #f1f1f1;
                padding: 10px;
            }

            input[type=text],
            input[type=number] {
                width: 100%;
                padding: 12px 20px;
                margin: 8px 0;
                display: inline-block;
                border: 1px solid #ccc;
                box-sizing: border-box;
            }

            input[type=submit] {
                width: 100%;
                background-color: #4CAF50;
                color: white;
                padding: 14px 20px;
                margin: 8px 0;
                border: none;
                cursor: pointer;
            }

            input[type=submit]:hover {
                background-color: #45a049;
            }

            label {
                font-weight: bold;
            }

            h1 {
                text-align: center;
            }
        </style>
</head>

<body>
    <header>
        <%- include("./partials/navbar") %>

            <h1>
                <% if(product._id){%>
                    Edit Product
                    <%}else {%>
                        New Product
                        <% } %>

            </h1>

    </header>
    <form method="post">
        <label>Recipies Name: <input type="text" name="name" placeholder="Enter product name"
                value="<%=product?.name%>" />
        </label>
        <br />
        <br />

        <label>Description: <input type="Text" name="price" placeholder="Enter product price" value="<%=product?.price%>" />
        </label>
        <br />
        <br />

        <label>ingredients: <input type="Text" name="qty" placeholder="Enter product quantity"
                value="<%=product?.qty%>" />
        </label>
        <br />
        <br />

        <label>instructions: <input type="text" name="manufacturer" placeholder="Enter Manufacturer"
                value="<%=product?.manufacturer%>" />
        </label>
        <br />
        <br />

        <% if(!product?._id) {%>
            <input type="submit" value="Save" formaction="/product/save" />
            <%}%>
                <% if(product?._id) {%>
                    <input type="submit" value="Update" formaction="/product/update/<%=product._id%>" />
                    <%}%>

    </form>
</body>

</html>